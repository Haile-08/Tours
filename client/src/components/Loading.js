import React from "react";

const Loading = () => {
  return (
    <div className="flex justify-center items-center font-mono text-5xl h-screen bg-slate-400">
      Loading
    </div>
  );
};

export default Loading;
